{
  "name": "kultivarfinder-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@fontsource/montserrat": "^4.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "validate": "node scripts/validate.mjs",
    "validate:backups": "node scripts/validate-backups.mjs",
    "validate:refs": "node scripts/validate-refs-online.mjs",
    "validate:data": "node scripts/validate.mjs",
    "audit:scripts": "node scripts/audit-scripts.mjs",
    "sync:backups": "node scripts/sync-backups.mjs",
    "build": "npm run validate && react-scripts build",
    "test": "react-scripts test"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
